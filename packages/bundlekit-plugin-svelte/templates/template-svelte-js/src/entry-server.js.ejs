import App from "./App.svelte";

/**
 * 服务端渲染入口
 *
 * @param {string} url 当前请求路径，用于路由匹配
 * @returns {string} HTML 字符串，将由 service 替换到 index.html 的 <!--ssr-outlet--> 占位
 */
export function render(url) {
  const { html } = App.render({ url });
  return html;
}
