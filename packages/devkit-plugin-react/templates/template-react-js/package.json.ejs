{
  "name": "<%= projectName %>",
  "version": "1.0.0",
  "description": "<%= description %>",
  "scripts": {
    "clean": "rimraf dist",
    "dev": "devkit-service serve --bundler webpack --mode development",
    "build": "devkit-service build --bundler webpack --mode production",
    "vite:dev": "npm run clean && devkit-service serve --bundler vite --mode development",
    "webpack:dev": "npm run clean && devkit-service serve --bundler webpack --mode development"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@devkit/service": "^0.0.1"
  }
}
