/**
 * Library 入口（library 模式由 generator 重命名为 src/index.tsx）
 *
 * 这个文件就是你的 SDK 公共 API：所有要被消费方 import 的东西都从这里 export。
 * 默认把 App 组件作为示例 SDK 暴露出去；按需加 hooks / utils / types 即可。
 */
import App from "./App";

export { App };

/** 简单的 SDK 工具示例 */
export function greet(name: string): string {
    return `Hello, ${name}! — from <%= libraryName %>`;
}

const <%= libraryName %> = { App, greet };
export default <%= libraryName %>;
